{
  "name": "supersurf-extension",
  "version": "0.6.3",
  "private": true,
  "description": "SuperSurf Chrome extension for browser automation",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc && npx tsx build.ts",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test.watch": "vitest",
    "zip": "zip supersurf-extension.zip ./assets/ ./dist/ ./manifest.json"
  },
  "devDependencies": {
    "typescript": "~5.5.0",
    "vitest": "^4.0.18"
  }
}
